program hello_world

/*
 * Services of the firmware
 */
@group(target)
interface hello_world_target {
    say_hello_to_target(string name) -> string
}

/*
 * Services of the host
 */
@group(host)
interface hello_world_host {
    say_hello_to_host(string name) -> string
}
